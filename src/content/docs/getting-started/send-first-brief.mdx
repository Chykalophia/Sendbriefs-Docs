---
title: Send your first brief
description: Use Add to a brief to contribute, fill in the four sections, submit through approval, send. About 15 minutes the first time. Two minutes the second time.
sidebar:
  order: 5
---

import Placeholder from '../../../components/Placeholder.astro';

<aside class="sb-tldr">
<p class="sb-tldr-label">TL;DR</p>
Click <strong>Add to a brief</strong> in the sidebar, pick a client, fill in the template's sections, send. The brief lands in your client's inbox and in their hosted portal. About 15 minutes the first time.
</aside>

## The mental model

A brief in SendBriefs is built around three ideas:

1. **A client and a period.** Briefs belong to a specific client over a specific reporting period (last month, last week, etc.). The "Add to a brief" flow resolves which draft brief is the right one to contribute to.
2. **Sections defined by the template.** A brief is organized into named sections. The starter templates ship with **Done / In Progress / To Do / Additional Notes** — but templates can rename, restyle, and rearrange them freely. The four defaults are a starting point, not a hard rule.
3. **Blocks inside sections.** Each section is a stack of blocks (text, headings, metrics, tables, callouts, images, charts). The editor's `/` slash-menu adds blocks.

Each brief moves through a state machine: `draft → pending_approval → approved → sent`, with `changes_requested` as a side path.

## Step 1: Click "Add to a brief"

In the left sidebar, under **REPORTS**, click **Add to a brief**. You'll land at `/reports/update`.

<Placeholder type="screenshot" caption="The Add to a brief landing page — client picker on the left, recent clients listed" />

## Step 2: Pick the client

If you arrived from a client page, the client is pre-selected. Otherwise, pick from the client list.

Once a client is selected, SendBriefs picks the right brief for you. Four possible outcomes:

#### If a draft exists for the current period
You land directly in the composer with an "Adding to: [brief title]" line.

#### If no draft exists yet
And the client has a configured template + cadence — SendBriefs creates a new draft brief for the period and drops you in.

#### If the period brief has already been sent or approved
You'll see a chooser offering three options: reopen the sent brief, start the next period's brief, or cancel.

#### If the client isn't on the Living period flow
This happens when the client has no template set, contributions are disabled, or the cadence isn't recurring. You'll see a chooser to start a one-off brief or pick a template.

For your first time, "start the next period's brief" is the common path if you haven't built one for this client before.

## Step 3: Fill in the sections

The editor shows the template's sections stacked, each collapsible. Click any section header to expand.

<Placeholder type="screenshot" caption="Brief editor — sections collapsed at the top, one expanded showing blocks inside" />

For each section, type your update or use **`/`** to open the block picker. Available block types include:

- **Paragraph, Heading, Quote, List, Checklist** — text and structure
- **Metric, Gauge, Sparkline, StatsGrid** — numeric callouts
- **Table** — tabular data
- **Image** — embedded screenshots or assets
- **Callout** — for wins, blockers, asks
- **Divider, Section, Column** — layout helpers
- **Sentiment, ChannelGrid** — specialized blocks

Empty sections render nothing in the final brief — only the sections you fill in show up to the recipient.

> **Layout mode: `strict` vs `flexible`.** In `strict` (default), every block lives inside a section. In `flexible`, some blocks can sit outside sections — useful for a hero callout at the top. Usually leave it alone.

## Step 4: Preview

Click **Preview** at the top of the editor. The brief renders the way the client will see it in their portal — branded chrome, your colors, your logo.

<Placeholder type="screenshot" caption="Preview pane showing the rendered brief with the client's branding" />

What to check:

- **Are the right sections populated?** Empty sections are hidden in the rendered output.
- **Do the numbers and dates make sense?** Most data in the current preview is written directly; data integrations are limited (see [Integrations](/features/integrations/)).
- **Does the branding match expectations?** Logo and accent colors.
- **Does the period header read correctly?**

## Step 5: Submit for approval (if your workspace requires it)

If your workspace requires approvals, click **Submit for approval**. The brief moves to `pending_approval` and assigned approvers get a `report_submitted` notification.

Approvers can:

- **Approve** — brief moves to `approved`. From here, you (or the approver) can send it.
- **Request changes** — brief moves to `changes_requested` with a note for the author. You fix and resubmit.

If approvals aren't required in your workspace, you'll see the **Send** option directly.

## Step 6: Sanity-check before sending

There's **no dedicated "send test to me" button on a brief** in the current preview. Three ways to sanity-check rendering before the client sees it:

### In-app preview
Use the Preview pane for the rendered version with full branding. Good for catching layout issues.

### Email layout sample
Settings → Email Layout → open a layout → **Email myself**. This sends a sample of the wrapper to your inbox with placeholder content — confirms the email renders correctly in real email clients (Outlook, Gmail, etc.).

### Smoke-test client
Create a client record with your own email on a `receive reports`-on contact. Send to that client first to see the actual brief in your inbox. Most teams use this pattern.

## Step 7: Send to the client

When the preview looks right:

1. Click **Send** on the brief.
2. Confirm the recipient list (every contact on this client with `receive reports` on).
3. Confirm.

The brief moves to `sent`. You'll see a confirmation, and the brief appears in the **Sent** tab on the Reports list.

> For recurring monthly briefs, scheduling lives at **Settings → Reports → Scheduling**, not on the individual brief. See [Monthly client reporting](/flows/monthly-reporting/) for the full schedule-and-forget workflow.

## Step 8: Watch delivery

Open the sent brief. Two cards show what happened:

- **Engagement bar** (top of the brief) — aggregate cadence, comment count, open count, open rate, and send time.
- **Delivery card** (below the brief) — recipient-by-recipient table with **Delivered** and **Opened** icons per row, and aggregate Recipients / Delivered / Opened totals.

Clicks are tracked but not shown as a column — open the per-recipient detail to see them. Portal views are tracked too but not shown in the UI yet.

Don't read too much into "0 opens" — many email clients block the tracking pixel. Treat opens as best-effort, deliveries as reliable.

## You did it

You shipped a brief. Every brief after this one is faster — the four sections become muscle memory, and your saved templates speed up the structure for new clients.

## Next: what comes next

[What's next →](/getting-started/whats-next/)
